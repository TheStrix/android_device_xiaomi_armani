allow mpdecision kernel:dir search;
allow mpdecision kernel:file { read open };

allow mpdecision socket_device:dir { write add_name remove_name };
allow mpdecision socket_device:sock_file { create setattr write unlink };

allow mpdecision thermal-engine:dir search;
allow mpdecision thermal-engine:file { read open };
